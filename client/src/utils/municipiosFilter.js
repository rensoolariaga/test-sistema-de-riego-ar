import { municipiosArg } from "./municipiosArg.js"

  console.log('soy el municipiosArg.municipios: ', municipiosArg.municipios)

  // buenos aires
  export let municipiosBuenosAires = municipiosArg.municipios.filter((m) => m.provincia.nombre === "Buenos Aires")

  municipiosBuenosAires = municipiosBuenosAires.map((m) => m.nombre);

  // catamarca  
  export let municipiosCatamarca = municipiosArg.municipios.filter((m) => m.provincia.nombre === "Catamarca")

  municipiosCatamarca = municipiosCatamarca.map((m) => m.nombre);

  // chaco
  export let municipiosChaco = municipiosArg.municipios.filter((m) => m.provincia.nombre === "Chaco")

  municipiosChaco = municipiosChaco.map((m) => m.nombre);

  // chubut
  export let municipiosChubut = municipiosArg.municipios.filter((m) => m.provincia.nombre === "Chubut")

  municipiosChubut = municipiosChubut.map((m) => m.nombre);

  // cordoba
 export let municipiosCordoba = municipiosArg.municipios.filter((m) => m.provincia.nombre === "Córdoba")

  municipiosCordoba = municipiosCordoba.map((m) => m.nombre);

  // corrientes
  export let municipiosCorrientes = municipiosArg.municipios.filter((m) => m.provincia.nombre === "Corrientes")

  municipiosCorrientes = municipiosCorrientes.map((m) => m.nombre);

  // entre rios NO ESTA
  export let municipiosEntreRios = municipiosArg.municipios.filter((m) => m.provincia.nombre === "Entre Ríos")

  municipiosEntreRios = municipiosEntreRios.map((m) => m.nombre);

  // formosa
  export let municipiosFormosa = municipiosArg.municipios.filter((m) => m.provincia.nombre === "Formosa")

  municipiosFormosa = municipiosFormosa.map((m) => m.nombre);

  // jujuy
  export let municipiosJujuy = municipiosArg.municipios.filter((m) => m.provincia.nombre === "Jujuy")

  municipiosJujuy = municipiosJujuy.map((m) => m.nombre);municipiosJujuy

  // la pampa
  export let municipiosLaPampa = municipiosArg.municipios.filter((m) => m.provincia.nombre === "La Pampa")

  municipiosLaPampa = municipiosLaPampa.map((m) => m.nombre);

  // la rioja
  export let municipiosLaRioja = municipiosArg.municipios.filter((m) => m.provincia.nombre === "La Rioja")

  municipiosLaRioja = municipiosLaRioja.map((m) => m.nombre);

  // mendozaaaaaaaaaaaaaaaaaaaa
  export let municipiosMendoza = municipiosArg.municipios.filter((m) => m.provincia.nombre === "Mendoza")

  municipiosMendoza = municipiosMendoza.map((m) => m.nombre);

// misiones
  export let municipiosMisiones = municipiosArg.municipios.filter((m) => m.provincia.nombre === "Misiones")

  municipiosMisiones = municipiosMisiones.map((m) => m.nombre);

  // neuquen
 export let municipiosNeuquen = municipiosArg.municipios.filter((m) => m.provincia.nombre === "Neuquén")

 municipiosNeuquen = municipiosNeuquen.map((m) => m.nombre);

  // rio negro
 export let municipiosRioNegro = municipiosArg.municipios.filter((m) => m.provincia.nombre === "Río Negro")

  municipiosRioNegro = municipiosRioNegro.map((m) => m.nombre);

  // salta
 export let municipiosSalta = municipiosArg.municipios.filter((m) => m.provincia.nombre === "Salta")

  municipiosSalta = municipiosSalta.map((m) => m.nombre);municipiosSalta

  // san juan
 export let municipiosSanJuan = municipiosArg.municipios.filter((m) => m.provincia.nombre === "San Juan")

  municipiosSanJuan = municipiosSanJuan.map((m) => m.nombre);

  // San Luis
 export let municipiosSanLuis = municipiosArg.municipios.filter((m) => m.provincia.nombre === "San Luis")

  municipiosSanLuis = municipiosSanLuis.map((m) => m.nombre);

  // Santa Cruz NO ESTA
 export let municipiosSantaCruz = municipiosArg.municipios.filter((m) => m.provincia.nombre === "Santa Cruz")

  municipiosSantaCruz = municipiosSantaCruz.map((m) => m.nombre);

  // Santa Fe
 export let municipiosSantaFe = municipiosArg.municipios.filter((m) => m.provincia.nombre === "Santa Fe")

  municipiosSantaFe = municipiosSantaFe.map((m) => m.nombre);

  // Santiago del Estero NO ESTA
 export let municipiosSantiagoDelEstero = municipiosArg.municipios.filter((m) => m.provincia.nombre === "Santiago del Estero")

  municipiosSantiagoDelEstero = municipiosSantiagoDelEstero.map((m) => m.nombre);

  //Tierra del Fuego
 export let municipiosTierraDelFuego = municipiosArg.municipios.filter((m) => m.provincia.nombre === "Tierra del Fuego")

  municipiosTierraDelFuego = municipiosTierraDelFuego.map((m) => m.nombre);

  // Tucumán
  export let municipiosTucuman = municipiosArg.municipios.filter((m) => m.provincia.nombre === "Tucumán")

  municipiosTucuman = municipiosTucuman.map((m) => m.nombre);
